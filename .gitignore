.venv*/
__pycache__/
*.pyc
.DS_Store
CLAUDE.md
node_modules/

# Render sources for the README artwork. They are generated by the private
# tooling repo (herdr-radar-extras: bin/banner.js, bin/sidebar.js) and point at
# screenshots on one machine, so the copy here is nobody's but the author's.
# What ships is the rendered result in assets/.
tools/banner.html
tools/sidebar.html
